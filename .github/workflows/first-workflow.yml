name: first workflow

on:
  push:
    branches:
      - main

jobs:
  - first-job:
    runs-on: ubuntu-latest 
    
    steps: 
      - name: Checkout code
        uses: actions/checkout@v4 
   
      - name: print
        run: echo "Hello, DevOps! 🚀"
