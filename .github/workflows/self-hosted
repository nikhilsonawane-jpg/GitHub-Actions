name: selfh hosted

on:
  push:

env:
  Name: nginx

jobs:
  install:
    runs-on: self-hosted
    steps:
      - name: Update and Install Nginx
        run: |
          sudo apt update -y
          sudo apt install ${{ env.Name }} -y
