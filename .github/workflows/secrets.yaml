name: secrets 
on:
  workflow_dispatch:
    inputs:
      secret:
        decsription: "Enter secret"
        required: true
env:
  n: ${{secrets.NAME}}
jobs:
  secrets:
    runs-on: ubuntu-latest
    steps:
      - name: print
        if: env.n == 'Nikhil'
        shell: bash
        run: | 
          echo "Hello Nikhil"
      - name: printt
        if: env.n != 'Nikhil'
        shell: bash
        run: | 
          echo "Hello you are unknown" 
