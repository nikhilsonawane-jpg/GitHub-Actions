name: selfh hosted

on:
  push:

env:
  Name: nginx

jobs:
  install:
    runs-on: self-hosted
    steps:
      - name: Update and Install Nginx
        run: |
          sudo apt update -y
          sudo apt install ${{ env.Name }} -y
      - name: install webpage
        run: | 
          <h1> Im Devops Enigneer now </h1> > /var/www/html/index.html
      - name: 
        run: |
          systemctl restart nginx 
